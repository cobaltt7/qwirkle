/**
 * @file This file gives typings to `./vue-class-component.js` which is served from `node_modules` by the backend. This is a `.d.ts` file
 *   and not a `.js` file so it is excluded from the compiled output, minimizing confusion.
 */

export * from "vue-class-component";
